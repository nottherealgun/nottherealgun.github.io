---
import "../styles/global.css";
import ProjectCategoryButton from "../components/ProjectCategoryButton.astro";

let buttons = [
  {
    icon: "mdi:controller",
    buttonText: "Games and Interactives",
    link: "/projects/interactives",
  },
  {
    icon: "mdi:checkbox-intermediate-variant",
    buttonText: "Interface Designs",
    link: "/projects/interfaces",
  },
  { icon: "mdi:drawing", buttonText: "Graphic Designs", link: "" },
  { icon: "mdi:art", buttonText: "Doodles and Sketches", link: "" },
];
---

<div class="flex flex-col m-auto">
  <div
    class="flex flex-col gap-6 items-center-safe justify-center h-svh bg-primary lg:gap-8"
  >
    <div class="flex text-secondary">
      <h1 class="text-2xl lg:text-4xl">My Projects</h1>
    </div>
    <div class="flex flex-row gap-4">
      <div class="flex flex-col gap-2 lg:grid lg:grid-cols-4">
        {
          buttons.map((button, index) => (
            <ProjectCategoryButton
              icon={button.icon}
              buttonText={button.buttonText}
              link={button.link}
              index={index}
            />
          ))
        }
      </div>
    </div>
  </div>
</div>
